[build]
builder = "nixpacks"
buildCommand = "cd /app && npm install && npm run build -w packages/shared && npm run build -w apps/web"

[deploy]
startCommand = "cd /app && npm run start -w apps/web"
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
